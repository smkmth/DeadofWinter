using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/// <summary>
/// at the moment this does nothing, it is attached to the zombie game objects
/// and in the future might contain a zombie scriptable object with zombie stats
/// like health and damage. to be added.
/// </summary>
public class Zombie : MonoBehaviour {
	public string ZombieLocation;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
